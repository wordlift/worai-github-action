name: Test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run shell tests
        run: |
          ./tests/run-worai.sh
          ./tests/install-worai.sh
